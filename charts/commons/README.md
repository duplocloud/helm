# Duplocloud Commons Chart  

A library of useful functions to generate Kubernetes the Duplocloud way.
This chart is not intended to be installed directly, but rather to be used as a dependency in other charts.
